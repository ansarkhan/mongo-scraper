<div class="container">
    <div class="jumbotron">
        <div class="row">
            <div class="col-6">
                <h1 class="display-4">Article Scraper</h1>
                <p class="lead">A simple node app that scrapes articles from Hacker News and saves them to a MongoDB.</p>
            </div>
            <div class="col-6">
            </div>
        </div>
    </div>

    {{!-- <div class="row">
        <div class="col-6">

            <h3>Burgers to be devoured...</h3>
            <ul>
            {{#each burgers}}
                {{#unless devoured}}
                    {{> burgers/burger-block devoured=false}}
                {{/unless}}
            {{/each}}
            </ul>
        </div>
        <div class="col-6">
            <h3>Devoured burgers...</h3>
            {{#each burgers}}
                {{#if devoured}}
                    {{> burgers/burger-block devoured=true}}
                {{/if}}
            {{/each}}
        </div>
    </div> --}}
<hr>
    <div class="row justify-content-center">
        <div class="col-12">
            <a href="/scrape"><button class="btn btn-primary">Scrape Articles</button></a>
            <a href="/articles"><button class="btn btn-success">Get Articles</button></a>
        </div>
    </div>
</div>

